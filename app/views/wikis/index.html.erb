<h1>My wikis (Index) (found in index.html.erb)</h1>

<table class="table table-bordered">
  <thead>
    <tr class="warning">
      <th>User</th>
      <th>Name</th>
      <th>Body</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @wikis.each do |wiki| %>
      <tr class="success">
        <td><%= wiki.user %></td>
        <td><%= wiki.name %></td>
        <td><%= wiki.body %></td>
        <td><%= link_to 'Show', wiki %> | 
        <%= link_to 'Edit', edit_wiki_path(wiki) %> |
        <%= link_to 'Destroy', wiki, method: :delete, data: { confirm: 'Are you sure?' } %> | 
        <%= link_to 'Add Collaborators', new_wiki_collaborators_path(wiki) %>
      </td>
      </tr>
    <% end %>
  </tbody> 
</table>

<br>
<% if current_user %>
  <%= link_to 'New', new_wiki_path %>
<% end %>

<%#[FIXME]= link_to 'Profile', user_profile_path %> 
<%# [FIXME]= link_to 'Sign out', user_signout_path %>
